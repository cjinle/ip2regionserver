package ip2regionserver

import (
	"fmt"
	"testing"
)

func TestSth(t *testing.T) {
	// region, err := NewRegioner()
	// if err != nil {
	// 	panic(err)
	// }
	// fmt.Printf("%T, %v", region, region)
	// fmt.Println(region.Search("114.114.114.114"))
	fmt.Println("test")
	GinStart()
}